var assert  = require('assert');
var request = require('../bootstrap').requestAPI;
var Orgs    = require('../../models/organisations');

describe('Organisation API', () => {

    xit('GET /api/orgs/ should return array of all organisations', done => {
        done();
    });

    xit('GET /api/orgs/:id should return organisation with id', done => {
        done();
    });

    xit('POST /api/orgs/ should create new organisation', done => {
        done();
    });

    xit('POST /api/orgs/:id should update organisation with id', done => {
        done();
    });

    xit('DELETE /api/orgs/:id should delete organisation with id', done => {
        done();
    });
});